@{
    ViewData["Title"] = "Estapar - Adicionar Carros";
    ViewBag.Title = "Estapar - Adicionar Carros";
}

<div class="col-lg-12">
    <div class="card m-b-30">
        <div class="card-body">
            <section>
                <form method="post" enctype="multipart/form-data" asp-controller="Carro"
                      asp-action="Insert">
                    <div class="form-group" style="font-size: large; float: left; margin-right: 30px">
                        <label for="txtMarcaCarro">Marca</label>
                        <input type="text" class="form-control" style="width: 300px" id="txtMarcaCarro" placeholder="Marca do Carro" value="" />
                    </div>
                    <div class="form-group" style="font-size: large; float: left; margin-right: 30px">
                        <label for="txtModeloCarro">Modelo</label>
                        <input type="text" class="form-control" style="width: 300px" id="txtModeloCarro" placeholder="Modelo do Carro" value="" />
                    </div>
                    <div class="form-group" style="font-size: large; float: left; margin-right: 30px">
                        <label for="txtPlacaCarro">Placa</label>
                        <input type="text" class="form-control" style="width: 300px" id="txtPlacaCarro" maxlength="7" placeholder="Placa do Carro" value="" />
                    </div>
                </form>
            </section>
        </div>
        <div class="card-footer">
            <div class="col-12">
                <div>
                    <a onclick="SalvaCarro()" class="btn btn-primary" style="float: right"><i class="feather-plus mr-2 right"></i>Adicionar Carro</a>
                </div>
            </div>
        </div>
    </div>
</div>


@section Scripts
{
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery.mask/1.14.0/jquery.mask.js"></script>
    <script>
        function SalvaCarro() {
            var t_marca = $("#txtMarcaCarro").val();
            var t_modelo = $("#txtModeloCarro").val();
            var t_placa = $("#txtPlacaCarro").val();
            location.href = '/Carro/Insert?marca=' + t_marca + '&modelo=' + t_modelo + '&placa=' + t_placa;
        }
    </script>
}